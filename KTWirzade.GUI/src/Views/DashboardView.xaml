<UserControl x:Class="KTWirzade.GUI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:KTWirzade.GUI.Controls"
             xmlns:wpf="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="700">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
      </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="0,0,0,12" Padding="20,16" CornerRadius="8" Background="{DynamicResource NoticeBoxBrush}" BorderThickness="1" BorderBrush="{DynamicResource ContentBoxBorderBrush}">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <wpf:SymbolIcon Symbol="Home" TextBlock.FontSize="22" Margin="0,0,12,0" VerticalAlignment="Center" TextBlock.Foreground="{DynamicResource ProgressBarBrush}" />
                    <StackPanel>
                        <TextBlock FontSize="20" FontWeight="Bold" Text="Bem-vindo ao KT WIRZADE" Foreground="{DynamicResource TextPrimaryBrush}" />
                        <TextBlock Name="SubtitleText" FontSize="12" Margin="0,2,0,0" Foreground="{DynamicResource TextSecondaryBrush}" Text="Sistema de otimizacao e personalizacao do Windows" />
                  </StackPanel>
              </StackPanel>
          </StackPanel>
      </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" FontSize="14" FontWeight="SemiBold" Text="Acoes Rapidas" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10" />

                <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,16">
                    <Border CornerRadius="8" Padding="16,14" Background="{DynamicResource FakeBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource ContentBoxBorderBrush}" Margin="0,0,6,0" Cursor="Hand" MouseLeftButtonUp="QuickAction_LoadPlaybook">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="40" Height="40" CornerRadius="20" Background="{DynamicResource StatusInfoSoftBrush}" Margin="0,0,12,0">
                                <wpf:SymbolIcon Symbol="DocumentArrowDown" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{DynamicResource ProgressBarBrush}" />
                          </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Text="Carregar Playbook" />
                                <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Text="Aplicar um .apbx ao sistema" />
                          </StackPanel>
                      </StackPanel>
                  </Border>

                    <Border CornerRadius="8" Padding="16,14" Background="{DynamicResource FakeBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource ContentBoxBorderBrush}" Margin="6,0,6,0" Cursor="Hand" MouseLeftButtonUp="QuickAction_Rollback">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="40" Height="40" CornerRadius="20" Background="{DynamicResource StatusWarningSoftBrush}" Margin="0,0,12,0">
                                <wpf:SymbolIcon Symbol="ArrowUndo" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{DynamicResource StatusWarningBrush}" />
                          </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Text="Reverter Alteracoes" />
                                <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Text="Desfazer playbook anterior" />
                          </StackPanel>
                      </StackPanel>
                  </Border>

                    <Border CornerRadius="8" Padding="16,14" Background="{DynamicResource FakeBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource ContentBoxBorderBrush}" Margin="6,0,0,0" Cursor="Hand" MouseLeftButtonUp="QuickAction_CheckUpdates">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="40" Height="40" CornerRadius="20" Background="{DynamicResource StatusSuccessSoftBrush}" Margin="0,0,12,0">
                                <wpf:SymbolIcon Symbol="ArrowSync" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{DynamicResource StatusSuccessBrush}" />
                          </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Text="Verificar Atualizacoes" />
                                <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Text="Buscar versao mais recente" />
                          </StackPanel>
                      </StackPanel>
                  </Border>
              </UniformGrid>

                <TextBlock Grid.Row="2" FontSize="14" FontWeight="SemiBold" Text="Playbooks em Cache" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,8,0,10" />

                <Border Grid.Row="3" CornerRadius="8" Padding="0" Background="{DynamicResource FakeBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource ContentBoxBorderBrush}" MinHeight="120">
                    <Grid>
                        <StackPanel Name="EmptyState" Visibility="Visible" Margin="20,30">
                            <wpf:SymbolIcon Symbol="CloudOff" FontSize="36" HorizontalAlignment="Center" TextBlock.Foreground="{DynamicResource TextSecondaryBrush}" Opacity="0.5" />
                            <TextBlock Text="Nenhum playbook em cache" FontSize="13" HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="{DynamicResource TextSecondaryBrush}" />
                            <TextBlock Text="Carregue um .apbx para comecar" FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0" Foreground="{DynamicResource TextSecondaryBrush}" Opacity="0.7" />
                      </StackPanel>

                        <ItemsControl Name="CachedPlaybooksList" Visibility="Collapsed" Margin="10">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="12,10" CornerRadius="6" Margin="0,0,0,6" Background="{DynamicResource SecondaryBrush}" BorderThickness="1" BorderBrush="{DynamicResource ContentBoxBorderBrush}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                          </Grid.ColumnDefinitions>
                                            <wpf:SymbolIcon Symbol="Document" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center" TextBlock.Foreground="{DynamicResource ProgressBarBrush}" />
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
                                                <TextBlock FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}">
                                                    <Run Text="v" /><Run Text="{Binding Version}" /><Run Text=" - " /><Run Text="{Binding SizeDisplay}" />
                                              </TextBlock>
                                          </StackPanel>
                                            <TextBlock Grid.Column="2" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Text="{Binding CachedAtDisplay}" />
                                      </Grid>
                                  </Border>
                              </DataTemplate>
                          </ItemsControl.ItemTemplate>
                      </ItemsControl>
                  </Grid>
              </Border>
          </Grid>
      </ScrollViewer>
  </Grid>
</UserControl>
